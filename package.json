{
    "license": "GPL-3.0-or-later",
    "repository": {
        "type": "git",
        "url": "git@gitlab.com:adleatherwood/bplus-composer.git"
    },
    "scripts": {
        "build": "ncp README.md package/README.md && tsc --project tsconfig.json",
        "test": "jest",
        "test-w": "jest --watchAll",
        "coverage": "jest --coverage"
    },
    "devDependencies": {
        "@types/jest": "^26.0.14",
        "jest": "^26.4.2",
        "ncp": "^2.0.0",
        "ts-jest": "^26.4.0",
        "typescript": "^4.0.3"
    }
}
